const botao = document.querySelector('#botao')

botao.addEventListener('click', () => {
    window.scroll({footer: window.innerHeight,behavior: "smooth"})
    botao.getElementsByClassName.display ='none'
})
